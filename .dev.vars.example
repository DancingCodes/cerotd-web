# Cloudflare local bindings (.dev.vars)
ADMIN_API_TOKEN=dev-admin-token

# Also create a local .env for Nuxt runtimeConfig:
# NUXT_ADMIN_API_TOKEN=dev-admin-token

# Optional canonical site URL for SEO
# NUXT_PUBLIC_SITE_URL=https://www.example.com
